# Relatorio de execucao

Status: nao executado neste ambiente.

Motivo:

- JMeter nao esta instalado localmente neste sandbox.
- Nao ha acesso para publicar no GitHub a partir daqui.

Resultado esperado quando executado:

- Fluxo finaliza com sucesso na pagina de confirmacao.
- Throughput proximo de `250 req/s`.
- `90th percentile` abaixo de `2000 ms`.
- `Error %` igual a `0`.

Conclusao:

- O criterio de aceitacao ainda precisa de execucao real para ser confirmado.

